---
// 다크/라이트 테마 토글. variant: 'plain'(사이드바 푸터) | 'bordered'(헤더)
const { variant = "plain" } = Astro.props as { variant?: "plain" | "bordered" };
const cls =
  variant === "bordered"
    ? "inline-flex items-center justify-center w-8 h-8 rounded-lg border border-base-300 text-base-content/70 hover:text-primary transition-colors"
    : "inline-flex items-center justify-center w-9 h-9 rounded-lg text-[#f7931e] hover:bg-[#f7931e]/10 transition-colors";
---

<button type="button" class={`js-theme-toggle ${cls}`} aria-label="Toggle theme" title="라이트/다크 모드 전환">
  <svg class="js-theme-sun hidden" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="12" cy="12" r="4"></circle>
    <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"></path>
  </svg>
  <svg class="js-theme-moon hidden" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
  </svg>
</button>

<script is:inline>
  (function () {
    if (window.__themeToggleInit) return;
    window.__themeToggleInit = true;

    function sync() {
      var dark = document.documentElement.getAttribute('data-theme') === 'upwise-dark';
      document.querySelectorAll('.js-theme-sun').forEach(function (el) { el.classList.toggle('hidden', !dark); });
      document.querySelectorAll('.js-theme-moon').forEach(function (el) { el.classList.toggle('hidden', dark); });
    }
    function setup() {
      document.querySelectorAll('.js-theme-toggle').forEach(function (btn) {
        if (btn.dataset.bound) return;
        btn.dataset.bound = '1';
        btn.addEventListener('click', function () {
          var cur = document.documentElement.getAttribute('data-theme');
          var next = cur === 'upwise-dark' ? 'upwise-light' : 'upwise-dark';
          document.documentElement.setAttribute('data-theme', next);
          localStorage.setItem('upwise-theme', next);
          sync();
        });
      });
      sync();
    }
    document.addEventListener('astro:page-load', setup);
    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', setup);
    else setup();
  })();
</script>

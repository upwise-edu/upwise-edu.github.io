# build output
dist/
.cache
# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# Astro
.astro/

# upwise 로고 원본 파일 (배포에 불필요 — public/ 에 변환본만 사용)
_upwise/
# 로컬 참고 문서 (비배포·비추적)
_docs/

# 애널리틱스 로컬 도구 (서비스 계정 키·집계 결과 — 커밋 금지)
_analytics/
